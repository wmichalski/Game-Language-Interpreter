forEachPlayer(!player.inPrison)
{
    player.score = player.score + roll(6)
}

forEachPlayer(player.inPrison)
{
    if(roll(4) == 1)
    {
        player.inPrison = 0;
    }
    else
    {
        player.sad = 1;
    }
}